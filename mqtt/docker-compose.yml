services:
  mosquitto-lab4:
    image: eclipse-mosquitto:2
    container_name: mosquitto-lab4
    restart: unless-stopped
    ports:
      - "1884:1883"
    volumes:
      - ./config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log
